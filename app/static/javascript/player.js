// Generated by CoffeeScript 1.8.0
(function() {
  var Player;

  Player = (function() {
    var currentRoll, isTurn, tempScore, totalScore;

    function Player(name) {
      this.name = name;
    }

    currentRoll = [];

    totalScore = [];

    tempScore = 0;

    isTurn = false;

    Player.prototype.getName = function() {
      return this.name;
    };

    Player.prototype.setTotalScore = function(amount) {
      return totalScore = amount;
    };

    Player.prototype.getTotalScore = function() {
      return totalScore;
    };

    Player.prototype.setTurn = function(t) {
      isTurn = t;
      return console.log("" + this.name + " turn is now " + isTurn);
    };

    Player.prototype.isTurn = function() {
      return isTurn;
    };

    Player.prototype.setTempScore = function(score) {
      return tempScore = score;
    };

    Player.prototype.addTempScore = function(score) {
      return tempScore += score;
    };

    Player.prototype.getTempScore = function(score) {
      return tempScore;
    };

    Player.prototype.saveRound = function() {
      this.setTempScore(0);
      return this.setTurn(false);
    };

    return Player;

  })();

  window.Player = Player;

}).call(this);
